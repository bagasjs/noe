/**
 *
 * `norn.h` ~ A simple rendering api abstraction designed for Noe
 *
 */
#ifndef NORN_H_
#define NORN_H_

typedef struct RenderContext RenderContext;
typedef struct RenderContextConfig {
} RenderContextConfig;

typedef struct RenderVertexBuffer RenderVertexBuffer;

typedef struct RenderIndexBuffer RenderIndexBuffer;

typedef struct RenderShader RenderShader;

typedef struct RenderPipeline RenderPipeline;

typedef struct RenderTexture RenderTexture;

typedef struct RenderFrameBuffer RenderFrameBuffer;

#endif // NORN_H_
