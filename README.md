# NOE
NOE is a Raylib like graphics library that I've implemented from scratch.

### Features
- Multiple platform: linux, windows
- Text rendering
- OpenGL based batch renderer

## TODO
- Creating a new rendering api abstraction (norn.h) with Vulkan as its backend
- Fixing native linux (x11) platform
- Adding native windows platform
- Adding EGL for OpenGL Context Loading in Linux
- Adding web platform
